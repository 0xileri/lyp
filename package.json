{
  "name": "lyp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A read-only risk guardrail that sits between a trading agent and Binance Agent OS. Returns ALLOW / ALLOW_REDUCED / BLOCK with reasons. Never places trades.",
  "engines": {
    "node": ">=20.11"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "demo": "node scripts/demo.js",
    "audit:tools": "node scripts/audit-tools.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "@modelcontextprotocol/sdk": "^1.19.1",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  }
}
